# 歴史ピックアップ画面 UI仕様ドラフト

*[service_design_document.md](service_design_document.md) 8-3節の仕様を、実際の画面仕様に落とし込むためのドラフト。[ui_spec_circle.md](ui_spec_circle.md)と対になる文書。まだHTML/実装には着手していない。*

---

## 0. 確定した方針（ユーザー回答より）

| 項目 | 決定内容 |
|---|---|
| 操作手段 | スワイプ専用。タップ代替ボタンは用意しない |
| テーマタグ | カードに出来事の所属テーマ名を表示する |
| 中断時の扱い | 進捗は保存しない。閉じる／リロードすると毎回リセットされる |
| ドラッグ中の視覚フィードバック | ラベル文字は出さず、色と傾きだけで方向を示す |

---

## 1. 画面全体のトーン：「ピックアップは冷たく、円環は温かい」

[ui_spec_circle.md](ui_spec_circle.md)で採用した「事実＝冷たい計器の色調（instrument/void系）」「体感＝温かい肌色調（skin/ember系）」という設計言語を踏襲する。歴史ピックアップ画面はカードの事実レイヤーだけを見せる画面なので、**基調は冷たい計器トーン（void背景・instrumentのグレー系）に統一**し、円環UI画面で初めて温もり（ember）が灯る、という対比を画面をまたいで維持する。ember色は、後述するドラッグ中の「input方向」フィードバックとしてのみ、ごく短く先取りして使う（＝「これを選ぶと、あとで温かくなる」という予告）。

---

## 2. レイアウト

- 上部:**ドット型インジケータ**（最大10個）。inputするたびに1つずつemberで塗りつぶされていく。まだinputしていない分は薄いinstrumentの輪郭のみの円。
- 中央:**出来事カード**（1枚）。カードのすぐ後ろに、次のカードの端がわずかに覗く程度の「重なり」を薄く描き、山（デッキ）が続いていることを示す。
- カード内部（上から順）:
  1. **テーマタグ**（左上に小さなピル型ラベル、例:「氷」）
  2. **年**（大きく強調、例:「1806」）— 円環UI画面で最終的にこの年が円周上の位置を決めることになるので、ここで年を印象づけておく
  3. **出来事名**
  4. **事実レイヤー本文**（詳細説明）
- 初回の1〜2枚のみ、カード下部に小さく操作ヒントを表示する（例:「↑ 記憶する　他方向 見送る」）。3枚目以降は表示しない（操作を覚えたユーザーの邪魔をしない）。スワイプ専用でボタンを置かない決定に伴い、最初の発見しやすさをこのヒントだけで担保する。

---

## 3. ジェスチャーとドラッグ演出

### 3-1. 判定ロジック

- ドラッグ中、指の移動量から「上方向への動き」と「それ以外」を判定する。具体的には、ドラッグベクトルの角度が**真上を中心とした±50度程度の扇形**に収まり、かつ縦方向の移動量が最小しきい値を超えていれば「input方向」、それ以外の角度（左・右・下・浅い斜め上）で移動量がしきい値を超えていれば「discard方向」とする。
- 判定は**指を離した時点の位置**だけで行うシンプルな距離ベースとし、スピード（フリックの勢い）までは見ない。速度を加味した物理的な「フリング」演出は本格的なアプリでは自然だが、実装・調整のコストに見合わないので、まずは距離ベースの判定にとどめることを提案する（後日、体験が硬いと感じたら速度判定を足す拡張は可能）。
- しきい値未満で指を離した場合はカードが中央へ弾んで戻る（キャンセル）。

### 3-2. 視覚フィードバック（ラベルなし版）

- カードは指の横移動量に応じてわずかに傾く（回転角は横移動量に比例、上限あり）。これは方向に関わらず常に起こる、素直な物理演出。
- 加えて、判定ロジックが「input方向」を認識している間だけ、カードの縁からember色の淡いグロー（発光）がにじみ出る。「discard方向」を認識している間は、instrumentの冷たいグレーのグローに変わる。中央付近（判定前）はどちらのグローも出さない。
- 指を離して確定した瞬間:
  - inputなら、カードはember色に包まれながら上方向へ即座に消える（円環UI画面での「ember bloom」演出の先取りとして、視覚的な一貫性を持たせる）。
  - discardなら、カードは指を離した方向へそのまま滑り落ちて消える（色は淡いinstrumentグレーのまま、演出は控えめ）。
- 次のカードは、消えたカードと入れ替わるようにフェード＋わずかな拡大で登場する。

---

## 4. 終了時の遷移

- 3つの終了条件（input10件到達／ユーザーが「次へ」／候補出来事を出し切る）のいずれでも、画面遷移の見た目は同じにする:ドット（またはカード）がすっと消え、円環UI画面へフェードでつながる。
- 「次へ」ボタンは、input 1件以上でドット表示の下あたりに現れる想定（常時カードと重ならない位置に置く）。

---

## 5. 未確定・要検討事項

- ドラッグ判定の具体的な角度（±50度）やしきい値（px）は仮値。実機で触ってみてから微調整が必要。
- カードが同時に何枚「重なって見える」演出にするか（今回は次の1枚の端が覗く程度と仮定したが、演出なしのほうがすっきりする可能性もある）。
- 操作ヒントを表示する枚数（今回は最初の1〜2枚と仮定）や、ヒントの具体的な文言・見た目（矢印アイコンにするか文字だけにするか）は未定。
- スワイプ専用としたため、スワイプ操作が困難なユーザー（運動機能の制約など）への配慮は現状ない。将来的にタップ代替ボタンやキーボード操作（矢印キー等）を追加する余地は残しておきたい。

---

*次のアクション候補:上記の未確定事項にフィードバックをもらう。方針が固まれば、テーマ選択画面のUI仕様（カードグリッドのレイアウト・選択状態の見せ方など）も同様に詰め、3画面すべての実装プロトタイプに進める段階になる。*
