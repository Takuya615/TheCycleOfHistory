# 円環UI 仕様ドラフト

*[service_design_document.md](service_design_document.md) の設計思想（頭頂を起点にへそを中心として時計回りに一周する円環、身体部位はアプリが指定しない）を、実際の画面仕様に落とし込むためのドラフト。まだHTML/実装には着手していない。*

---

## 0. 確定した方針（ユーザー回答より）

| 項目 | 決定内容 |
|---|---|
| 全体レイアウト | スマホ表示前提・画面いっぱいの大きな円 |
| 出来事の間隔（位置） | 実際の年の間隔に比例して配置（均等割りではない） |
| タップ時の表示順 | 体感描写（体感レイヤー）を先に表示し、事実（年号・出来事名）は別操作で表示 |
| 探索の自由度 | 自由探索（古い順に解放する等の制限なし、状態管理不要） |

以下は上記4点を具体的な画面仕様に展開したもの。特に☑️マークがない項目はドラフトとしての提案であり、フィードバックが必要。

---

## 1. 円のジオメトリ

- **起点と向き**: 画面の12時位置（真上）を西暦1500年、そこから時計回りに西暦2000年まで進む。角度 = `(出来事の年 - 1500) / (2000 - 1500) * 360°`。
  - この計算方法だと、最新の出来事（例:1996年）は12時位置の少し手前（約357°）に来る。最後の出来事の「にじみ」が「円環の始まりへ還っていく」という文面（[content_ice.md](contents/content_ice.md)・[content_coffee.md](contents/content_coffee.md)で採用済み）と、視覚的にもほぼ一周して戻ってくる形が一致する。
- **円のサイズ**: `min(画面の横幅, 画面の縦幅) × 0.8` 程度を直径の目安とし、上下にヘッダー（テーマ名）とごく簡単な余白を確保する。ポートレート（縦持ち）表示を主対象とする。
- **円自体の意味**: 円周のライン自体は装飾（へそを中心とした身体の周囲という比喩）であり、実際に触れる操作対象は円周上に配置された「出来事の点」のみ。

## 2. 出来事の点（ドット）

- 各出来事は円周上に1つの点として表示する。点のラベル（出来事名や年号）は常時表示せず、タップして初めて内容が開く（画面いっぱいの大きな円という方向性を活かし、常時ラベル表示による文字の密集を避ける）。
- **タップ領域の最小サイズ**: モバイルのタップ目標として一般的な目安（44×44pt前後）を下回らないようにする。円周上の視覚的な点自体は小さくても、タップの当たり判定はその目安を満たす円形領域を確保する。
- **年が近い出来事の重なり対策**（提案・要確認）:比例配置だと、実際の年が近い出来事同士（例:1953年と1965年）は円周上でも近接し、タップ領域が重なる可能性がある。対策案:
  - (a) 点の視覚位置はあくまで正確な年の角度に置きつつ、タップ領域だけを必要な最小間隔までずらす（視覚的な位置の正確さを優先）
  - (b) 近接する点は円の半径方向に軽くオフセットして視覚的にも分離し、細い引き出し線で本来の角度位置と結ぶ
  - 現状10〜11件/テーマ・500年間隔であれば深刻な重なりは出にくいが、今後テーマによっては特定の年代に出来事が集中する可能性があるため、方針だけ先に決めておきたい。

## 3. タップ時の挙動

1. 出来事の点をタップすると、画面下部からボトムシート（またはモーダルカード）がせり上がり、**体感レイヤーのみ**を表示する（触れた瞬間→数秒後→にじみ、の3行）。
2. ボトムシート内に、事実レイヤー（年号・出来事名・説明）を表示するための明示的な操作（例:「これは何の出来事？」ボタン、またはタップでめくる/スワイプで切り替える）を用意する。ユーザーが望んだときだけ知識層に降りられるようにし、[service_design_document.md](service_design_document.md) の「感覚を先に、知識は後から」という設計思想を保つ。
3. ボトムシートを閉じる（背景タップ・下スワイプ・×ボタンなど）と円環表示に戻り、他の点を自由な順番でタップできる。
4. 一度見た点に視覚的な既読マーク（色を変える、薄いチェックなど）を付けるかどうかは任意。付ける場合も、状態はページの再読み込みで消えて構わない簡易な見た目上の演出とし、localStorageなどでの永続化は行わない（DBなし・状態管理なしの方針に合わせる）。

## 4. スマホでの見え方（詳細）

- 縦持ち（ポートレート）を主対象。横持ちは今回のドラフト範囲外（必要であれば別途検討）。
- ヘッダー領域:テーマ名（例:「歴史の宮殿 - 氷」）を上部に薄く表示する程度に留め、円の存在感を最優先にする。
- ボトムシートの高さ:画面の6〜7割程度を目安とし、円の一部が背景に見える状態を保つ（円環の中にいる感覚を途切れさせない）。
- ピンチズーム・パンなどの複雑な操作は今回のスコープに含めない(円のサイズとタップ領域の設計で吸収する)。

## 5. 未確定・要確認事項

- 「年が近い出来事の重なり対策」は (a) と (b) のどちらを採用するか
- 既読マーク（見た点の色を変える等）を入れるか、入れないか
- 円のビジュアルトーン（線の太さ・色・テーマごとに変えるか統一するか）は、[frontend-design](frontend-design) 的な検討がまだ手つかず。デザイン方向性を詰めるフェーズを別途設けるか、実装時に一緒に決めるか

---

*次のアクション候補:上記の未確定事項にフィードバックをもらう。方針が固まれば、実際にHTML/CSS/JSでの実装（1〜2テーマ分のデータを使ったプロトタイプ）に進める段階になる。*
