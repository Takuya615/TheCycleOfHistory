# 身体で歴史を記憶するアプリ——サービス設計書（概要編）

---

## 1. サービスの概要

このアプリは、歴史的な出来事を「知識」としてではなく「身体感覚」として記憶することを目的とした、学習支援Webアプリケーションである。

ユーザーは、時系列に並んだ歴史的出来事を学ぶとき、それぞれの出来事に対応するテキストをAIが生成した固定コンテンツとして受け取る。出来事は古い順から新しい順へ、頭頂を起点としておへそを中心に時計回りで身体の周囲を一周する、円環状のタイムラインとしてイメージされる。画面上では中央に描かれた一本の円環に、出来事が年代順に配置される形で表示される。円環上のどの位置が実際の身体のどの部位に対応するかはアプリ側では指定せず、ユーザー自身の想像に委ねられる。記憶したい順番に出来事を「体に貼り付けていく」ような体験を通じて、歴史の流れを直感的・身体的に把握できる。

コンテンツには2つの型がある。1つは「肌に触れるマテリアルの感覚」を核とする汎用テーマ型（氷・コーヒー・紙など、身の回りのモノの歴史）、もう1つは実在の偉人の功績を「Before／After」と「もし歴史が違ったら」という対比・反実仮想の構造で伝える偉人型（2026-09-21追加、詳細は4-4節）である。現在アプリのピックアップ画面はデモとして偉人型テーマ（「超有名な偉人」26人）のみに固定されているが、汎用テーマ型のデータ自体は45テーマ分がすでに同梱されている（9-0節参照）。

---

## 2. 解決したい問題

歴史の学習において、多くの人がぶつかる壁は「覚えたつもりが思い出せない」という現象である。年号・人名・出来事名を羅列で暗記しても、それらはバラバラな情報として脳に格納され、文脈や順序の感覚が伴いにくい。

特に日本の歴史教育では、時代の「流れ」よりも「点」の記憶が優先されがちであり、出来事の体験的なリアリティが失われやすい。このアプリはその問題に対して、認知科学と記憶術の知見を活用して応答する。

---

## 3. 目的

- 歴史的出来事を、身体感覚と結びつけることで長期記憶への定着を促す
- 時系列の「順序」を、身体という物理的な空間に対応させることで直感的に把握させる
- 「知っている」ではなく「感じたことがある」という記憶の質を目指す

---

## 4. 使用している記憶・認知のテクニック

### 4-1. 場所法（Method of Loci）

**概要**
場所法とは、古代ギリシャ・ローマ時代から伝わる記憶術であり、記憶したい情報を想像上の「場所」に配置することで再現性の高い記憶を形成する手法である。「記憶の宮殿」とも呼ばれる。

**このアプリでの応用**
想像上の建物や道ではなく、ユーザー自身の身体を「場所」として使用する。アプリが提示するのは、頭頂を起点としておへそを中心に時計回りで身体の周囲を一周する円環状の位置関係のみであり、その円環上の各位置を実際の身体のどの部位（足裏・膝・腹・胸・肩・首など）に対応させるかはユーザー自身が決定する。具体的な部位選びを委ねることで、既存の身体感覚・身体イメージとの結びつきをより個人的なものにし、自分の体という最も身近で忘れられない「場所」を使うことで、場所の想起コストをゼロに近づけている。

**有効性の根拠**
場所法は、世界記憶選手権の上位入賞者の多くが採用する手法であり、無関係な情報の列を大量に記憶するタスクにおいて、何も使わない暗記と比較して再現精度が有意に高いことが複数の実験で示されている（Maguire et al., 2003）。特に「空間的な順序」が手がかりとして機能することで、想起時のアクセスが容易になる。

*（2026-09-25追加）具体的な部位選びを完全にユーザーの想像に委ねるという上記の原則に対し、初回コーチマーク（8-6節）では「1500年は頭」「1600年は左手」「1700年は左足」「1800年は右足」「1900年は右手」という大まかな対応の一例を最初に一度だけ提示するようにした。これは自由な想像に委ねるだけでは最初の一歩に迷うユーザーがいるため、あくまで出発点となる大枠の型を示す補助であり、8-1節のピクトグラム自体や、円環上の細かな配置・4-5節の個々の`bodyPart`はこれまで通りこの大枠に縛られない。

---

### 4-2. 具体的なマテリアルと強烈な肌感覚による符号化（Sensory Encoding, 2026-08-29改訂）

*（本節は汎用テーマ型コンテンツに適用される技法である。偉人型コンテンツについては4-4節を参照）*

**概要**
人間の記憶は、抽象的な言葉よりも、具体的な感覚情報と結びついた場合に定着しやすい。これは符号化特異性原理（Tulving & Thomson, 1973）として知られており、記憶の保存時と想起時の文脈が一致するほど再現率が上がるとされる。

**このアプリでの応用**
AIは各歴史的出来事について、その出来事の周辺情報（時代・地域・関連するモノや行為）から連想を飛躍させ、多少突飛でも構わないので「マテリアル」——具体的な情景・人物・モノ——を1つ強制的に創り出す。そのうえで、そのマテリアルが肌に触れる・働きかけてくる場面から生じる**強烈な肌感覚**（痛み・重さ・不快感・極端な高温など）を描写する。

たとえば「1723年、コーヒーの木がカリブ海に持ち込まれる」という出来事であれば、史実の内容をそのまま感覚化するのではなく、「カリブ海→海賊」という連想の飛躍を経て「潮に汚れた海賊船長がナイフを肌に突き立ててくる」という具体的なマテリアルを創り、そこから「肌が焼けるように痛く、潮の匂いが鼻に張り付く」という強烈な肌感覚を導く。この「具体的な情景 → 強烈な肌感覚」という流れ自体が記憶の「フック」として機能する。

**有効性の根拠**
感覚的に鮮明な刺激は、海馬と扁桃体の両方を活性化させ、感情記憶と陳述記憶が統合されることで長期記憶に移行しやすくなることが神経科学的に示されている（McGaugh, 2000）。特に痛みや不快感を伴う強い刺激は扁桃体の活性化が大きく、より強く記憶に刻まれやすい。また、複数の感覚モダリティを同時に使った符号化（マルチモーダル符号化）は、単一モダリティよりも記憶の定着率が高い。

---

### 4-3. 認知的ひっかかり（Cognitive Friction）の意図的導入

**概要**
「快適で流れるような情報」よりも、「少し違和感のある情報」のほうが記憶に残りやすい。これをDesirable Difficulty（望ましい困難）と呼ぶ（Bjork, 1994）。

**このアプリでの応用**
AIへの指示として「心地よさよりも、痛み・重さ・不快感・極端な高温といった強烈な刺激を伴う感覚を優先する」というルールを設定している。また、固有名詞や数字を使わないことで、知識として処理することを防ぎ、感覚として受け取らせる。マテリアル自体も、実在の人物や史実そのものの直接描写ではなく、連想によって生まれた汎用的なキャラクター・情景にとどめる（著作権上の理由に加え、事実レイヤーとの役割分担を保つため）。

**有効性の根拠**
心理学者Robert Bjorkの研究によれば、学習時に適度な負荷をかけることで長期的な記憶定着が向上する。違和感や強い刺激は「予測誤差」を生み出し、脳がその情報を「重要なもの」として優先処理するためである。

---

*（旧バージョンでは、この後に「隣接する感覚のにじみ」という4-4節があり、体感描写を「触れた瞬間」「数秒後」「にじみ」の3段階構成にして出来事間の連続性を表現していた。2026-08-29の方針転換により、この3段階構成と「にじみ」による連続性の演出は廃止し、「マテリアル」と「肌感覚」の2段構成に一本化した。）*

---

### 4-4. 対比構造と反実仮想による因果理解の符号化（偉人型コンテンツ、2026-09-21追加）

**概要**
4-1〜4-3節の技法は「モノ」を主語にした強烈な肌感覚の符号化を狙うものだが、これとは別に、実在の偉人の功績を扱う出来事群（テーマID:`greatman`、「超有名な偉人」）向けに、もう1つの符号化パターンを導入した。世界的に知られた人物16人と、日本の歴史的偉人10人の計26人分のコンテンツが、`content_greate_man.md`の原稿から`data/events/events_greatman.json`として実装済みである。

**このアプリでの応用**
偉人型の出来事は、汎用テーマ型と異なり「具体的なマテリアル」＋「強烈な肌感覚」の2段構成ではなく、以下の要素で構成される（詳細な項目定義は9-2節参照）。

- **Before／After**：その偉業が起こる前の社会状況と、起こった後にどう変わったかを対比させる
- **もし歴史が違ったら（反実仮想）**：その偉業が「もし起きていなかったら」「もっと早く／遅く起きていたら」を、前後の別の史実と結びつけて考察する（史実そのものではなく、あくまで一つの可能性としての提示であることを明記する）
- **象徴アイコン（絵文字1つ）**：本人の有名な逸話・比喩・発明品・持ち物・ジェスチャーなど、調べればすぐ「あの人だ」とわかる要素から選ぶ。選定ルールは`マテリアル提案用プロンプト.txt`に準拠し、アイコンそのものおよびその選定理由（iconReason）の文章には固有名詞・数字・身体部位語を使わない。ただし出来事のタイトル（人物名）自体は実名を用いる——これは4-3節の「固有名詞禁止」ルールに対する、この型に限った例外である
- 強烈な肌感覚の描写（体感レイヤー）は持たない。円環UI上のマテリアルの役割は、象徴アイコンと因果関係の物語（Before/After・反実仮想）が担う

**有効性の根拠**
歴史的な出来事を「その出来事がなかったら／別の形だったら世界はどうなっていたか」という反実仮想で問い直すことは、単なる年号や事実の暗記ではなく、出来事同士の因果関係の理解を促す。これは7章で述べる「なぜそれが起きたのか」を問い直すというサービスの目的そのものに直結する符号化パターンであり、Before/Afterの対比は変化量そのものを際立たせることで記憶のフックを作る。

---

### 4-5. 身体部位への具体的なマッピングによる記憶強化（偉人型コンテンツ、2026-09-23追加、2026-09-24全面改訂）

**概要**
4-1節の場所法は、円環上のどの位置がどの身体部位かを**ユーザー自身の想像に委ねる**ことを原則としている。偉人型コンテンツに限り、この原則を補強する形で、AIが各出来事の象徴アイコンを**具体的にどの身体部位でどう感じればよいか**まで示す2つのフィールドを追加している——短いラベルの「体の部位」（`bodyPart`）と、一文で描写する「記憶しやすくするためのイメージ」（`mnemonicImage`）。ユーザーの想像の自由を奪うのではなく、覚え方に迷ったときの具体案として提示する位置づけである（なお、円環UI画面のピクトグラム自体の見た目・ポーズは8-1節の通り完全に装飾であり、この身体部位マッピングとは連動していない）。

**身体部位マッピング（コンテンツ制作時の目安、2026-09-24改訂）**

正面から見た人体の「輪郭線」をなぞるように、頭頂部を起点に時計回りで一周（1500〜2000年）し、**10年刻み・全50箇所**で身体部位に対応させる。1500年と2000年は同じ頭頂部で重なる。

| 区間 | ルート（10年刻み） |
|---|---|
| 1500→1600（頭部→左手、頭の輪郭から腕の上側をたどる） | 頭頂部→頭部左側→左耳→左耳の下→左首すじ→左肩の上→左二の腕(肩寄り)→左二の腕(ひじ寄り)→左ひじ→左前腕→左手の先 |
| 1600→1750（左手→股下、腕の下側から脇腹・こし・脚の輪郭へ） | 左手の先→左手首→左脇腹(上)→左脇腹(下)→左こし→左こしの下→左外もも(上)→左外もも(下)→左外ふくらはぎ(上)→左外ふくらはぎ(下)→左外くるぶし→左足つま先→左内くるぶし→左内ふくらはぎ→左内もも→股下 |
| 1750→1900（股下→右手、1600-1750の左右反転） | 股下→右内もも→右内ふくらはぎ→右内くるぶし→右足つま先→右外くるぶし→右外ふくらはぎ(下)→右外ふくらはぎ(上)→右外もも(下)→右外もも(上)→右こしの下→右こし→右脇腹(下)→右脇腹(上)→右手首→右手の先 |
| 1900→2000（右手→頭部、1500-1600の反転） | 右手の先→右前腕→右ひじ→右二の腕(ひじ寄り)→右二の腕(肩寄り)→右肩の上→右首すじ→右耳の下→右耳→頭部右側→頭頂部（1500年と同一） |

おへそは通らない。股下（1750年）が唯一の体の中心線上のポイントで、そこから先は左右反転して右半身をたどる。

**このアプリでの応用**
各出来事の実際の西暦を最も近い10年刻みのポイントに丸め、複数の出来事が同じポイントに重なる場合は前後どちらのポイント寄りかで自然に描き分ける（例：ジェンナー1796年→右足の親指のつま先、ナポレオン1804年→右足の小指のつま先、のように同じ「右足つま先」ポイントでも部位を微調整する）。`bodyPart`にはこの部位を短いラベルで、`mnemonicImage`にはアイコンが表す物・人物・動物などがその部位で「どう位置し、何をしているか」を一文で描写する。例：

- マゼラン（1522年、🧭）→ `bodyPart`:「左耳の穴」、`mnemonicImage`:「左耳の穴に羅針盤が埋め込まれているイメージ。」
- ダーウィン（1859年、🐒）→ `bodyPart`:「右こし」、`mnemonicImage`:「右のこしに猿がしがみついているイメージ。」

固有名詞・数字は使わないという4-3節のルールはここでも維持するが、iconReason（アイコンの選定理由）とは異なり、この2項目は**身体部位語の使用を前提とする**（8-1節・9-2-2節参照）。

---

## 5. AIプロンプトの設計思想

このアプリの核心は、歴史的出来事と身体部位を入力として受け取り、感覚描写を出力するAIプロンプトにある。設計の主要な判断は以下の通りである。

| 設計上の判断 | 意図 |
|---|---|
| 具体的な身体部位はアプリが指定せず、円環上の位置（年代順）のみを渡す | 部位と意味の対応はユーザー自身の想像に委ね、事後的に発見する体験にする |
| 出来事の周辺情報から連想を飛躍させ、具体的な「マテリアル」（情景・人物・モノ）を1つ強制的に創出させる | 抽象的な感覚描写だけでは記憶のフックとして弱いため、具体的で強烈なイメージを作る |
| そのマテリアルから生じる強烈な肌感覚（痛み・重さ・不快感・極端な高温など）を描写させる | 心地よさより刺激の強さを優先し、記憶定着を狙う（望ましい困難の強化版） |
| 固有名詞・数字の使用を禁止する（マテリアルも実在の人物・著作物のキャラクターを名指しせず、連想から生まれた汎用的な存在にとどめる） | 知識処理ではなく感覚処理を促す。事実レイヤーとの役割分担を保ち、著作権上のリスクも避ける |
| センシティブな史実（戦争・迫害・強制労働など）では、連想の飛躍先を実際の苦難そのものから意図的に逸らし、無関係な架空のモチーフに向ける | 強烈な刺激を狙いつつ、実在の悲劇を娯楽的に描写してしまうリスクを避ける（課題5） |

*（2026-09-21追加）上記の表は汎用テーマ型コンテンツのプロンプト設計を対象とする。偉人型コンテンツ（4-4節）では、出来事の主語は実在の人物であることを前提とするため「固有名詞禁止」ルールは人物名（タイトル）自体には適用せず、マテリアル（象徴アイコン）とその選定理由の文章にのみ適用する。またセンシティブな出来事（戦争・投獄など）については、出来事発生から50年未満など当事者・関係者が存命の可能性がある場合にその旨を「要レビュー注記」に明記したうえで、実際の被害・暴力ではなく関連する物や象徴（鎧・鎖など）に描写を留める運用としている。*

---

## 6. 想定ユーザーと活用シーン

- 歴史の試験勉強をしている学生
- 教科書的な暗記が苦手で、体験的な学習を好む人
- 歴史を大局的な流れとして把握したい社会人
- 歴史教育に新しいアプローチを探している教育者

---

## 7. このサービスが目指すもの

知識は「知っている」状態をつくるが、感覚は「体験した」状態をつくる。歴史上の出来事は過去のものであり、直接体験することはできない。しかしこのアプリは、身体というもっとも個人的な空間に歴史を「貼り付ける」ことで、知識と体験の間にある溝を少しだけ埋めようとする。

覚えることが目的ではなく、「なぜそれが起きたのか」を問い直すきっかけをつくること——それがこのサービスの本当の目的である。

---

## 8. アプリの画面構成とユーザーフロー（2026-08-29 追記、2026-09-21 大幅改訂）

### 8-0. 全体の流れ

このアプリは3つの画面のマークアップを持つが、**現在はテーマ選択画面をコメントアウトして無効化しており、実質的に2画面構成で動作する**（2026-09-21のコンテンツ刷新でワークフローを変更。データはすべて端末のローカルストレージにのみ保持し、サーバー・DBは持たない方針は変わらない。詳細は9章）。

1. **円環UI画面（メイン画面）** — 起動時に開く画面。端末に保存された「円環リスト」（ユーザーが選び取った出来事の一覧）を円環上に表示する。
2. **テーマ選択画面（現在無効化）** — ホワイトリストのテーマから興味のあるものを最大5つまで選ぶ画面。実装（HTML/JS）自体はコメントアウトした形で残しており、将来の再有効化に備えている。
3. **歴史ピックアップ画面** — 候補出来事から2枚（残り1枚のときは1枚）をランダムに提示し、どちらかをタップして選ぶ「デュエル方式」の画面。レベルの保有上限に達するまでこれを繰り返し、記憶したいものを円環リストに加えていく。2026-09-21に「1枚ずつスワイプでinput/discard」方式から「世紀ごとにカードを一覧表示して複数選択」方式へ、2026-09-23にさらに現行の「2択デュエル」方式へ作り直した（8-3節参照）。

テーマ選択画面が無効化されたことに伴い、「作成」または「追加」ボタンは直接テーマ選択を経由せず歴史ピックアップ画面へ遷移する。遷移先のテーマはコード上の定数`DEMO_THEME_IDS`（現在は`['greatman']`＝「超有名な偉人」1本のみ）で固定されており、デモとして偉人型コンテンツのみを見せる状態になっている。テーマ選択画面が無効な間は、歴史ピックアップ画面からの「もどる」手段もない（以後は一方向）。

```
円環UI画面 ──(「作成」または「追加」をタップ)──▶ 歴史ピックアップ画面
     ▲                                                  │
     │                                　  固定テーマ（現状はgreatmanのみ）の未保有出来事から
     │                                　  ランダムに2枚（残り1枚なら1枚）を提示
     │                                                  │
     │                                    タップした方を1件選ぶ（即座に次のラウンドへ）
     │                                                  ▼
     │                        レベルの保有上限に達するまでラウンドを繰り返す（10-3節）
     │                                                  │
     └───────────（全ラウンド終了後、選んだ分を円環リストへ一括追加）────┘
                                       円環UI画面（内容が更新される）
```

*（テーマ選択画面を再有効化する場合は、旧仕様どおり「作成/追加」タップ→テーマ選択（複数選択可・最大5つ、もどるボタンあり）→歴史ピックアップ、という3画面フローに戻す想定。テーマ選択画面自体の仕様は8-2節に残してある）*

### 8-1. 円環UI画面（メイン画面）

- 端末の円環リストが **null（＝一度もピックアップしたことがない）** の場合、円環中央に **「作成」ボタン** を表示する。
- 円環リストが **null以外（＝1件以上ある）** の場合、円環中央に **「追加」ボタン** を表示する。
- ボタンのラベルが違うだけで、どちらをタップしても遷移先は同じ（現状は8-0節の通り歴史ピックアップ画面へ直接遷移する）。
- （2026-09-22実装）クイズフェーズ導入に伴い、円環リストの件数が現在のレベルの保有上限に達している間だけ、中央ボタンの遷移先がクイズフェーズに切り替わる（ボタンのデザインも変わる）。保有上限を下回っている間は従来通りピックアップ遷移のまま。詳細は10章参照。
- 円環リストにある出来事は、選んだテーマを問わず1つの円環上に年代順で混在表示される（テーマごとに区切らない）。
- 円環の内側は**白で塗りつぶし**、その上に頭・胴体・両腕・両脚からなる人体シルエット（ピクトグラム）を不透明度100%でくっきりと重ねて表示する（2026-08-29導入、2026-09-24改訂：旧仕様では不透明度約16%の薄い装飾だったが、視認性を上げるため白地にくっきりシルエットという構成に変更）。シルエットの色は、配色パレットごとの`--instrument-dim`（もとの薄い装飾で使っていた色そのもの）を維持する——2026-09-24改訂の初期版では円環の外側と同じ背景色を試したが、イメージと異なるとのフィードバックによりもとの色に戻した。円環上のどの位置がどの部位かを具体的に指定しない、という原則自体は維持する（4-1節）。
  - **ポーズ**（2026-09-24改訂、同日中に2回再修正）：両腕は肩から左右**真横**に伸ばす一直線（肘で折れない単純な線分、自然な長さ）。一度は「手の先を厳密に1600年/1900年の座標に届かせる」実装を試し、その次に「1600年/1900年の角度の方向へ伸ばす（水平よりやや上向き）」実装を試したが、いずれもフィードバックにより見送り、最終的に最もシンプルな左右水平の腕に落ち着いた。両腕の先端を円環上の特定の年に対応させる仕様自体は撤回しており、現状は完全に装飾的な大の字ポーズである。
  - **各部の比率**（2026-09-24改訂、簡易的なピクトグラム／案内サインのプロポーションを参考に調整）：頭部（円）・首の隙間・胴体はいずれも頭部の直径を基準にした自然な比率とし、**おへそ（胴体の下端）を円環の中心に固定**する。両脚は（従来どおり）中心から円環の枠線手前まで伸ばす。脚の先は円環の枠線に届かせず、頭部は胴体から離して浮かせて配置する（胴体の線には丸い線端があり、見た目上その分だけ隙間が埋まってしまうため、実際の隙間の指定値はその線端の太さも見込んで大きめに取っている）。
  - **胴体の太さ**（2026-09-25追加）：胴体の線は、腕・脚と同じ太さでは目立たなかったため、腕・脚の1.5倍の太さで描く。太くした分、頭部との隙間（上記）も丸い線端の分だけ広げて調整している。
- 円環上に保持している出来事はそれぞれ、アイコン（またはドット）のすぐ外側に**その出来事の西暦を小さく表示**する（2026-09-23追加）。
- **重なったノードのクラスター表示**（2026-09-25追加）：出来事同士の西暦が近く（角度差9°未満＝約12.5年以内）、隣接する出来事どうしを鎖のようにたどってひとまとまりにできる場合、そのグループ内のどの出来事のタップ判定円も重ならないよう、通常は1つの**件数バッジ**（例：「3」）としてまとめて表示する。バッジをタップすると、そのグループの出来事だけを個別にタップできるよう間隔をあけて展開する（主に放射方向、SVGの表示領域からはみ出さない範囲でメンバー数に応じて間隔を調整し、あわせて外側のメンバーほど角度もわずかに振ることで、3件以上でも指で区別しやすい間隔を確保している）。展開した状態でバッジ以外の場所（円環の背景・ピクトグラムなど）をタップすると、元の件数バッジ表示に戻る。展開中にメンバーの1つをタップすればそのまま出来事の詳細シートが開き、クラスターの展開状態はそのまま保持される（詳細シートを閉じても自動では畳まない）。旧仕様（〜2026-09-25）では、近接する出来事を常に小さく（15px刻みで）放射方向へずらして重なりを避けようとしていたが、アイコンのタップ判定円（半径32px）に対してずらす幅が小さすぎ、実際にはタップ判定が重なったままでタップしづらいという問題があったため、この方式に置き換えた。
- 100年区切りの目盛り（1500〜1900年の5箇所）は、円環を横切る目盛り線に加えて、中心から円環へ向かう薄い放射状の区切り線（スポーク）を表示し、円環がどの100年区分に分かれているかをひと目でわかりやすくしている（2026-09-23追加）。
- 出来事の詳細を表示するボトムシートの内容は、出来事の型によって異なる（4-4節・4-5節・9-2節参照）。
  - **汎用テーマ型**：年・あらすじ・場所に続けて「事実レイヤー」「具体的なマテリアル」「体感レイヤー」「選定理由」を表示する。
  - **偉人型**（2026-09-25改訂）：年に続けて、人物アイコン＋**人物名（フルネーム、`fullName`）**・**記憶しやすくするためのイメージ**（4-5節）・場所を表示し、本文には「何をした人か」「詳細（事実レイヤー）」「Before」「After」を表示する。人物名をフルネームで表示するのはこの詳細シートのみで、ピックアップ画面・クイズの問題文では引き続き通称の`title`（例：「ニュートン」）を使う。旧仕様にあった「アイコンの選定理由」「もし歴史が違ったら（反実仮想）」は詳細シートから削除し、代わりにBefore/After（もともと歴史ピックアップ画面のカードで見せていた対比）を表示するように変更した（`iconReason`・`counterfactual`のデータ自体はマスタに残しているが、画面には出さない）。体感レイヤーに相当する項目はない。
  - ボトムシート**最下部の「削除ボタン」**はどちらの型でも共通。タップすると、その出来事を円環リストから外す（＝ローカル保存データからID参照を1件除外するだけで、出来事マスタ本体には一切手を加えない）。一度外した出来事は、以後の歴史ピックアップ画面で再び候補カードとして出てきてよい（＝ユーザーは同じ出来事を後から円環に戻すことができる）。
- 画面右上の歯車アイコンから**配色（パレット）設定パネル**を開閉できる（2026-09-21追加、8-4節参照）。
- 円環中央のラベルは、円環リストが1件以上ある場合のみ「触れて、感じてください」というヒントを白いピル型の背景とともに表示する。円環リストが空（`null`）の場合は、ピクトグラムの視認性を妨げないよう中央ラベル自体を非表示にする（2026-09-25改訂：旧仕様では空の場合も「まだ何も記憶していません」という文言を同じピル背景で表示していたが、ピクトグラムの邪魔になるとのフィードバックにより削除した）。

### 8-2. テーマ選択画面（2026-09-21現在：コメントアウトにより無効化）

*実装（HTML/JS）はindex.html内にコメントアウトした形で残っており、下記の仕様定義もその実装に対応する。現在の画面遷移では使用されない（8-0節参照）。*

| 項目 | 内容 |
|---|---|
| ヘッダー文言 | 「興味のあるテーマを選択してください（複数選択可）」 |
| レイアウト | ホワイトリストのテーマを2列グリッドのカードで一覧表示（**表示順はランダム**。毎回シャッフルする） |
| 選択方式 | 複数選択可・最大5つまで |
| 次へボタン | 1つ以上選択した時点でアクティブ化。タップで歴史ピックアップ画面へ遷移 |
| もどるボタン（2026-09-06追加） | 次へボタンの左に常時表示。タップで選択内容を破棄し円環UI画面へ戻る |

### 8-3. 歴史ピックアップ画面（2026-09-23：2枚から1枚選ぶ「デュエル方式」へ作り直し）

対象テーマは、8-0節の通り現状`DEMO_THEME_IDS`（`['greatman']`＝「超有名な偉人」）に固定されている。テーマ選択画面が再有効化された場合は、ここで扱う候補出来事はユーザーが選んだ1〜5テーマ分に置き換わる想定。

- 候補出来事は、対象テーマの出来事のうち**現在の円環リストに入っていないものだけ**を対象とする（一度円環から削除した出来事は、以後のピックアップ画面で再び候補になり得る点は変更なし）。世紀によるグルーピングは廃止し、候補全体から無作為に抽出する。
- **ラウンド数は「現在のレベルの保有上限 − 円環リストの件数」**（＝そのピックアップで新たに追加できる件数、10-3節）で決まる。ただし候補出来事の総数がそれより少ない場合は候補数を上限とする。
  - ヘッダーに「現在のラウンド数 / 総ラウンド数」を表示し、クイズ画面と同じ形式のドット型インジケータで通過済み・現在地・未通過を示す。
- 各ラウンドでは、その時点でまだ選ばれていない候補からランダムに**2枚**（残り候補が1枚のときは1枚）を提示する（2026-09-24修正：カードの間にあった「または」の区切り表示は削除し、カード同士を直接並べる形にした）。
- カードは**横並び**に配置する。2枚のときは画面幅を2等分して**横スクロールなしで両方とも表示しきる**ことを基本とし、カード幅・肖像画サイズ・文字サイズを画面幅に応じて縮小する（2026-09-24修正：横並びにした直後はカード幅が固定的で、スマホ幅だと横スクロールが必要になってしまっていたため、画面幅に応じて可変にした）。文言が長い場合など収まりきらない場合の保険として横スクロールも残しており、PCではマウスホイール縦スクロールを横送りに変換し、ドラッグでも横送りできる補助動作を持つ（2026-09-23導入）。
- カードを**タップした方がそのラウンドで選ばれ、即座に次のラウンドへ進む**（選択の確定ボタンはない。旧仕様の「複数選択→次へ」方式は廃止）。そのラウンドで選ばれなかった方のカードは破棄されず、以後の別ラウンド・別セッションで再び候補になり得る。
- カードの表示内容は出来事の型によって異なる（4-4節・9-2節の型区分に対応）。アイコン・肖像画は、いずれもカード内で**中央寄せ**に表示する（2026-09-23修正。旧仕様では左寄りになっていた）。
  - **偉人型カード**（2026-09-25再改訂）：上から「肖像画」「人物名＋西暦」「覚え方（アイコン＋アイコン名＋体の部位）」の順に並べる。「人物名＋西暦」は実名の`title`をそのまま使い（例：「マゼラン（1522年）」）、「覚え方」は小見出しラベルの下にアイコン・アイコン名・体の部位を並べて表示する（例：「覚え方　🧭 羅針盤・左耳の穴」）。アイコン名は出来事マスタの`iconReason`（9-2-2節）の先頭部分（アイコンの日本語名）をそのまま流用し、体の部位は`bodyPart`（9-2-2節、4-5節）を使う——どちらもデータ自体は追加のフィールドではなく既存フィールドの再利用で、`title`自体は変更しない。キャッチーなあらすじ・「Before」「After」・事実レイヤーはここでは見せず、円環UI画面の詳細シートで見るお楽しみとして取っておく。
    - （旧仕様の変遷）2026-09-24時点では「アイテム名の人物名」という愛称（例：「羅針盤のマゼラン」）と体の部位を2行の文として繋げる表示だったが、実名と覚え方の情報を分けたほうが分かりやすいというフィードバックにより現行の構成に改めた。
  - **汎用テーマ型カード**：西暦・場所・タイトル・キャッチーなあらすじに続けて、事実レイヤーを表示する。具体的なマテリアル・体感レイヤーはここでは見せない（旧仕様から変更なし）。
- 総ラウンドをすべて終えると、選ばれたカードのIDをまとめて円環リストへ追加日時つきで一括追加し、ローカル保存したうえで円環UI画面へ遷移する（追加された出来事は10-5節の演出で明滅する）。
- この画面に「もどる」手段はなく、ラウンドを進むだけの一方向フロー（8-0節参照）。

### 8-4. 設定パネル（配色テーマ切り替え、2026-09-21追加）

- 円環UI画面右上の歯車アイコンから開閉する、全画面共通のオーバーレイパネル。
- **6種類の配色パレット**（琥珀の余韻／深夜の書斎／石灰岩の回廊／藍色の残響／黒板〈桃色チョーク〉／黒板〈黄色チョーク〉）から1つを選ぶ。デフォルトは「琥珀の余韻」。
- 選択結果は`<html>`要素の`data-palette`属性に反映され、CSS変数（背景色・強調色など）を切り替える。選択は端末のlocalStorageに保存され、次回起動時にも引き継がれる。
- 機能・画面フローには影響しない、見た目のみの設定項目。

### 8-5. 文字サイズ・アイコンサイズ（2026-09-24改訂）

スマートフォンでの視認性向上のため、アプリ全体（円環UI・歴史ピックアップ画面・クイズ画面・出来事詳細シート・設定パネル・チュートリアル）の文字サイズとアイコンサイズを引き上げた。特に円環UI上の保有年号ラベル（9px→13px）とノードアイコン（20px→26px、あわせてノードの当たり判定・グロー半径も拡大）は旧仕様で極端に小さかったため、優先的に拡大している。

### 8-6. 初回オンボーディングとコーチマーク（2026-09-24追加）

初めてアプリを開いたユーザー向けに、2段階のガイド機能を用意している。データはいずれも端末のlocalStorageにフラグとして保存し、一度見たら再表示しない（設定パネルの「使い方を見る」からはいつでも手動で再生可能）。

- **導入カルーセルモーダル（初回のみ自動表示）**: 「身体を場所として使う記憶術」の考え方を、静的イラスト（`image/onboarding/step1.png`〜`step8.png`）を1枚ずつ見せながら説明する全10枚構成のカード型モーダル。「スキップ」でいつでも中断でき、最後の「はじめる」で閉じる。閉じた時点でフラグを保存し、直後に次のFABコーチマークへつなぐ。
- **コーチマーク（画面上の要素をスポットライト表示する操作ガイド）**: 対象要素の位置に追従する強調リングと吹き出しを表示する。複数ステップを順番に見せる「シーケンス」に対応しており、現在は次の2つのタイミングで使用する。
  1. 円環が空の状態で以下を順に表示する（初回のみ）。(a) 円環全体をハイライトして「円のフチが体の部位に対応します。」と伝える。(b) 各世紀の年号ラベル（1500〜1900）を1つずつ順にハイライトし、「1500年は頭」「1600年は左手」「1700年は左足」「1800年は右足」「1900年は右手」と大まかな対応の一例を伝える（4-1節参照）。(c) 再び1500年のラベルをハイライトして「そして、2000年は頭にもどってきます」と一周したことを伝える。(d) 最後にFABボタンをハイライトして「ここから、最初の出来事を選びましょう」と伝える（2026-09-25、(a)〜(c)を追加）。(a)〜(c)は「どこをタップしても次へ進む」モードで表示する。
  2. 歴史ピックアップ画面でカード群をハイライトし、選び方を伝える（初回のみ）。
  3. ピックアップ完了後、以下の連続ステップを表示する（初回のみ）。(a) 円環全体をハイライトして「選んだ出来事が、円環にセットされました」と伝える。(b) 今回追加された出来事のノードを1つずつ順にハイライトし、「`<偉人名>`ゆかりのものは**`<アイテム名>`**、**`<西暦>年`**の出来事なので**`<体の部位>`**におくイメージ」と、その出来事固有の人物名・マテリアル・年・身体部位（9-2節の`title`／`iconReason`からアイコン絵文字を除いた部分／`year`／`bodyPart`）を差し込んで説明する（2026-09-25追加、アイテム名・西暦・体の部位の3箇所は太字表示。当初は主語を「この偉人」としていたが、どの人物の出来事か分かりやすいよう実名表示に変更）。(c) 最後にFABボタンをハイライトして「覚えたら、クイズに挑戦しましょう」と伝える。これら全ステップは「どこをタップしても次へ進む」モードで表示する。これは、ノードのタップを通常どおり素通りさせてしまうと出来事の詳細シートが開いてコーチマークの続きを確認できなくなる問題があったため（2026-09-24修正、2026-09-25にノード個別ステップへも適用）。同じ理由で、FABボタンをハイライトする最後のステップでも、コーチマーク表示中のタップはFAB本来のクリック（クイズ開始）を発火させず、コーチマークを閉じるだけに留める。

**不具合修正（2026-09-25）**：吹き出し（`.coach-tip`）に常時`pointer-events:auto`が指定されていたため、コーチマークを閉じたあと（オーバーレイ自体は`opacity:0`で見えなくなる）も、吹き出しは最後に表示されていた座標に透明なまま留まり、クリックを奪い続けていた。コーチマークは画面をまたいで同じ`#coachOverlay`要素を使い回すため、例えばピックアップ画面のコーチマーク（カード群の下に表示）やFABボタンのコーチマーク（画面下部中央に表示）を一度でも見ると、その座標がたまたま別の画面（クイズ画面の選択肢ボタン、クイズ結果画面の「つづける」ボタンなど、画面下部中央〜下寄りに置かれたボタン全般）と重なり、「タップしても反応しないことがある」という不具合につながっていた。オーバーレイに`.show`クラスが付いている間だけ吹き出しの`pointer-events`を有効化するよう修正し、他の類似コンポーネント（`.scrim`・`.onboard-scrim`・`.settings-panel`・`.sheet`）も同種の不具合がないか併せて点検し、問題ないことを確認した。

---

## 9. マスタ一覧（想定データ構造）

DBは持たないため、以下はすべて **アプリに同梱される静的データ（テーママスタ・出来事マスタ）** と、**端末のローカルストレージにのみ保存される動的データ（円環リスト）** の2種類に分かれる。

### 9-0. データの物理形式とロード方針（2026-09-06 追記）

`index.html` へのインライン埋め込みは避け、別途JSONファイルとして持ち、`fetch`で取得する方針とする。

| データ | ファイル | ロードタイミング |
|---|---|---|
| テーママスタ | `data/themes.json`（45件、軽量なので一括） | アプリ起動時に一括ロード（テーマ選択画面で常に全件表示するため） |
| 出来事マスタ | テーマごとに分割:`data/events/events_<テーマID>.json` | 画面遷移のタイミングで必要な分だけ遅延ロード（下記） |

出来事マスタの遅延ロードの具体的な挙動:

- **円環UI画面（メイン画面）起動時**:端末の円環リストに含まれる出来事IDの接頭辞（テーマID）を集計し、重複を除いた上でそのテーマ分の`events_<テーマID>.json`だけをfetchする（円環リストに登場しないテーマのJSONは取得しない）。
- **歴史ピックアップ画面への遷移時（2026-09-21改訂）**:テーマ選択画面が無効化されている現状は、`DEMO_THEME_IDS`定数で固定された1テーマ分（現状`greatman`のみ）の`events_<テーマID>.json`をfetchしてから、世紀ごとのグルーピング・カード表示を開始する。テーマ選択画面を再有効化した場合は、選択した1〜5テーマ分を並行fetchする設計に戻す想定（テーマ単位で分割されているが、選択済みテーマ分はまとめて先読みする）。
- 一度fetchしたテーマのJSONはセッション中メモリ上にキャッシュし、同じテーマへ再度戻った場合の再フェッチを避ける。

なお、`contents/content_<theme>.md`（コンテンツ試作・執筆用のプロレイヤー原稿）と`data/events/events_<theme>.json`（アプリが実際に読み込む機械可読マスタ）は別物である。原稿からJSONへの変換は`.claude/skills/history-palace-compile`スキルが担う（2026-09-06実装）。同様に`theme_whitelist.md`の内容は`data/themes.json`として書き起こし済み（45件）。2026-09-21現在は45件すべての有効フラグがtrueになっているが、テーマ選択画面自体が無効化されているため、この一括ロードの結果はテーマ選択画面が再有効化されるまで画面上では使われない。

### 9-1. テーママスタ（静的・アプリ同梱）

`theme_whitelist.md` の内容をアプリ内のデータとして持たせたもの。実データは `data/themes.json` に格納する。

| フィールド名 | 型 | 内容 |
|---|---|---|
| テーマID | string | 例:`ice`, `coffee`, `paper`（`content_<slug>.md`のslug、および`events_<slug>.json`のファイル名と対応） |
| テーマ名 | string | 例:「氷」 |
| カテゴリ名 | string | 例:「自然素材の歴史」 |
| 選定理由 | string | ホワイトリスト検討時の理由（表示は任意） |
| 主な感覚要素 | string | 例:「冷たさ・硬さ・溶解」 |
| 有効フラグ | boolean | テーマ選択画面に出すかどうか |

### 9-2. 出来事マスタ（静的・アプリ同梱、2026-09-21 項目改訂：偉人型スキーマを追加）

`content_<theme>.md` の各出来事をアプリ内のデータとして持たせたもの。実データはテーマごとに分割し `data/events/events_<theme>.json` に格納する。

2026-09-21のコンテンツ刷新により、出来事マスタには**汎用テーマ型（9-2-1）**と**偉人型（9-2-2）**の2つのスキーマが存在する。アプリ側は`counterfactual`フィールドの有無で型を判定する（`ev.counterfactual !== undefined`なら偉人型）。歴史ピックアップ画面のカード表示も同様に`before`フィールドの有無で分岐する。両者は同じ`data/events/events_<theme>.json`という物理フォーマットを共有するが、テーマ単位で型が統一されており、1つのJSONファイル内に型が混在することは想定していない。

#### 9-2-1. 汎用テーマ型（氷・コーヒー・紙など、45テーマ中`greatman`以外の44テーマ）

| フィールド名 | 型 | 内容 |
|---|---|---|
| 出来事ID | string | 形式:`<テーマID>-<西暦>-<3桁連番>`（例:`ice-1550-001`）。同一テーマ内で一意。**一度公開したら不変**（9-4節の運用ルール） |
| テーマID | string | 出来事IDの接頭辞から導出する（マスタ内に別項目としては持たない） |
| 西暦 | number | 1500〜2000。円環上の角度計算に使用 |
| タイトル | string | 出来事の見出し |
| キャッチーなあらすじ | string | 歴史ピックアップ画面のカードで興味を掻き立てるための短い煽り文 |
| 場所 | string | 出来事が起こった場所。史料の確度に応じて地名・地域など粒度は自由記述 |
| 事実レイヤー | string | 詳細な説明（歴史ピックアップ画面・円環UI画面の両方で表示） |
| 具体的なマテリアル | string | 連想から生まれた具体的な情景・人物・モノの描写（円環UI画面のタップ時に表示） |
| 体感レイヤー | string | そのマテリアルから生じる強烈な肌感覚（痛み・重さ・不快感・極端な高温など。同上） |
| 選定理由 | string | なぜそのマテリアル・体感レイヤーを選んだかの解説（円環UI画面の出来事詳細で表示） |
| 要レビューフラグ | boolean | センシティブな出来事かどうかの内部マーカー（ユーザー非表示） |
| 要レビュー注記 | string \| null | フラグがtrueの場合の説明（ユーザー非表示、運用者向け） |
| 参考ソースURL | string[] | 年代検証に使った出典 |

#### 9-2-2. 偉人型（テーマID:`greatman`「超有名な偉人」専用、2026-09-21追加）

`content_greate_man.md`（世界の偉人16人＋日本の偉人10人、計26人分の原稿）から`data/events/events_greatman.json`として実装済み。4-4節の技法に対応するスキーマで、汎用テーマ型の「具体的なマテリアル」「体感レイヤー」に相当する項目を持たない。

| フィールド名（JSON上のキー） | 型 | 内容 |
|---|---|---|
| 出来事ID（`id`） | string | 形式:`greatman-<西暦>-<3桁連番>`（例:`greatman-1522-001`）。一意・不変（9-4節） |
| テーマID（`themeId`） | string | 固定値`greatman` |
| 西暦（`year`） | number | 円環上の角度計算・世紀グルーピング（8-3節）の両方に使用 |
| アイコン（`icon`） | string | 本人と直結する象徴を表す絵文字1つ。26人全員で重複しないよう選定されている |
| タイトル（`title`） | string | 人物名（実名、通称ベース）。汎用テーマ型と異なり、この型では固有名詞禁止ルールの対象外（5章参照）。ピックアップ画面・クイズの問題文・円環UI詳細シートの見出しなど、`title`を使う箇所は引き続きこちらを使う |
| フルネーム（`fullName`、2026-09-25追加） | string | 人物のフルネーム（例:「アイザック・ニュートン」）。日本の偉人10人は`title`と同一（姓名がもともとフルネーム）。円環UI画面の出来事詳細シートの人物名表示にのみ使用し、それ以外の画面では`title`（通称）のまま |
| キャッチーなあらすじ（`teaser`） | string | 歴史ピックアップ画面のカードで興味を引く短い煽り文 |
| Before（`before`） | string | その偉業が起こる前の社会状況（2026-09-25改訂：円環UI画面の出来事詳細でも表示するようになった） |
| After（`after`） | string | その偉業が起こった後にどう変わったか（2026-09-25改訂：円環UI画面の出来事詳細でも表示するようになった） |
| 何をした人か（`whatTheyDid`） | string | 功績の1行要約（円環UI画面の出来事詳細で表示） |
| 事実レイヤー（`factLayer`） | string | 詳細な説明（円環UI画面の出来事詳細で表示） |
| もし歴史が違ったら（`counterfactual`） | string | 反実仮想の考察。史実そのものではなく一つの可能性としての提示であることが前提。マスタには残るが、2026-09-25改訂で円環UI画面の出来事詳細からは削除した（画面には出さない） |
| 場所（`place`） | string | 出来事が起こった場所・国 |
| アイコン選定理由（`iconReason`） | string | なぜそのアイコンを選んだかの解説。マスタには残るが、2026-09-25改訂で円環UI画面の出来事詳細からは削除した（画面には出さない） |
| 体の部位（`bodyPart`、2026-09-24追加） | string | 4-5節の身体部位マッピングに基づく短いラベル（例:「左耳の穴」）。歴史ピックアップ画面のカードで西暦とセットで表示（8-3節） |
| 記憶しやすくするためのイメージ（`mnemonicImage`、2026-09-23追加、2026-09-24全面改訂） | string | アイコンが身体のどの部位に位置し、何をしているかを一文で表す記憶術用の文章（4-5節）。`bodyPart`と同じ部位に基づいて書かれている。円環UI画面の出来事詳細で、人物名と肖像画の間に表示 |
| 要レビューフラグ（`needsReview`） | boolean | センシティブな出来事かどうかの内部マーカー（ユーザー非表示） |
| 要レビュー注記（`reviewNote`） | string \| null | フラグがtrueの場合の説明（ユーザー非表示、運用者向け） |
| 参考ソースURL（`sourceUrls`） | string[] | 年代検証に使った出典 |

### 9-3. 円環リスト（動的・端末ローカル保存のみ）

DBには保存しない。端末のlocalStorage等にID参照のみを保存する軽量な形式。

| フィールド名 | 型 | 内容 |
|---|---|---|
| 出来事ID | string | 出来事マスタへの参照（テーマIDは出来事IDから引ける） |
| 追加日時 | string(ISO8601) | いつピックアップされたか（円環UI上での既読管理や将来の並び替えに使える可能性） |

円環UI画面は起動のたびに、この参照リストを出来事マスタと突き合わせて実際の表示内容（年・事実レイヤーなど、9-2節の型に応じた項目一式）を組み立てる。テーマ名や出来事本文そのものは保存しない（ユーザー回答により参照のみ保存方式を採用）。

### 9-4. 確定した運用ルール・残る未確定事項（2026-08-29 確定、2026-09-21 一部更新）

2026-08-29のやり取りで、以下の3点が確定した:

- **候補出来事を出し切った場合**:（2026-09-21時点で廃止）旧スワイプ方式では、discard/input済みで候補が0件になった時点で自動的にピックアップを打ち切る仕様だったが、8-3節の年代ステップ方式への刷新に伴いこの概念自体がなくなった。現在は各世紀ステップの候補が0件でも空状態メッセージを表示したまま次のステップへ進めるだけで、ピックアップ自体が候補切れで打ち切られることはない。
- **テーマ選択画面の並び順**:ランダム表示（8-2参照。画面自体は現在無効化されているが仕様としては維持）。
- **円環リストからの削除**:円環UI画面の出来事詳細ボトムシート最下部に削除ボタンを設置し、ローカル保存データからID参照を外すだけの操作とする（8-1参照）。削除された出来事は、以後のピックアップ画面で再び候補になり得る。

なお、出来事マスタ本体（`content_<theme>.md` の内容）を将来編集・削除する場合の運用ルールは、以下を前提とする:出来事IDは一度公開したら基本的に不変とし、内容の誤り修正はテキストの差し替えのみで行う（IDの再利用・削除は避ける）。ユーザーの円環リストはID参照のみを保持するため、この運用を守る限り参照切れは発生しない。

残る未確定事項:

- 歴史ピックアップ画面の「デュエル方式」（8-3節、2026-09-23）の細かいビジュアル仕様（カード提示時のアニメーション演出など）は、`ui_spec_circle.md`と同様に別途UI仕様として詰める余地がある。
- テーマ選択画面を再有効化するタイミングと、その際に歴史ピックアップ画面の対象テーマ（現状`greatman`固定）をどう切り替えるか。

### 9-5. プレイヤー進行状況（動的・端末ローカル保存、2026-09-22実装）

円環リスト（9-3節）とは別に、クイズフェーズ（10章）の導入に伴い、レベルの進行状況を端末のローカルストレージに保持する。

| フィールド名 | 型 | 内容 |
|---|---|---|
| 現在のレベル | number | 初期値1。クイズ合格のたびに+1される。保有上限・称号はこのレベル番号から10-3節のテーブルを参照して導出するため、称号自体は別途保存しない |

---

## 10. クイズフェーズの設計（2026-09-22追加・実装済み）

*本章は8〜9章で説明した現行実装（円環UI画面・歴史ピックアップ画面）に、新たに「クイズフェーズ」を追加するための設計である。2026-09-22時点で`index.html`側の実装が完了している（レベル制・出題ロジック・結果演出まで、10-6節参照）。*

### 10-0. 全体像とレベル制

このアプリは「歴史ピックアップフェーズ」と「クイズフェーズ」を交互に繰り返すことで、プレイヤーが少しずつ26人の偉人の歴史を円環に集めていくレベル制を採る。

- プレイヤーは「レベル」を持ち、レベルごとに円環に保有できる出来事の上限数が決まっている（10-3節）。
- 円環リストの件数が現在のレベルの上限に**達していない**間は、ホーム画面（円環UI画面）の中央ボタンは従来通り歴史ピックアップ画面へ遷移する（8-1節）。
- 円環リストの件数が現在のレベルの上限に**達している**間は、中央ボタンの遷移先がクイズフェーズへの遷移ボタン（デザインを切り替える）に変わる。
- クイズフェーズで合格すると、レベルが1つ上がり、保有上限が開放される。ホーム画面のボタンは再びピックアップ遷移に戻り、開放された分だけ新しい出来事を選べるようになる。
- クイズフェーズで不合格の場合、レベルは変わらず、称号も変わらない（「あなたは◯◯のままだ」）。中央ボタンはクイズ遷移のままで、何度でもすぐに再挑戦できる（新しい5問がその都度ランダム生成される）。
- 円環リストから出来事を削除した場合、保有件数がレベル上限を下回るため、中央ボタンは自動的にピックアップ遷移に戻る（＝削除して別の出来事と入れ替えられる）。
- 全26件を保有した状態（レベル15到達後）は、それ以上ピックアップできる出来事が存在しないため新規追加はできない。クイズ自体は保有件数に関わらず何度でも周回できる（特別な画面表示は本設計では保留、2026-09-22時点）。

### 10-1. 出題ロジック（2026-09-25全面改訂）

- 1回のクイズフェーズにつき5問出題する。5問すべてに回答するまで正誤フィードバックは表示しない（10-2節）。全問、選択式（◯×形式は廃止、10-1-2節参照）。
- 1〜4問目は10-1-1節の10パターンからランダムに選ぶ（同じパターンの重複可）。5問目のみ10-1-2節の「出来事の体の部位」問題を出題する。
- 1〜4問目の出題内容（西暦・人物名・アイコン・体の部位など）は、円環リストに含まれる保有出来事（`events_greatman.json`）の中からランダムに構成する。事前に問題文を書き下ろした静的な問題集は持たず、`data/quiz/`配下には出題パターンの定義（`data/quiz/patterns.json`）、5問目用の静的マスタ（`data/quiz/order_events.json`、10-4節）、身体部位マッピングの静的マスタ（`data/quiz/body_points.json`、4-5節の50箇所をそのままJSON化したもの）を置く（2026-09-22実装、2026-09-25に体の部位を使うパターンを追加）。

#### 10-1-1. 設問パターン（1〜4問目）

| パターン | 問題文の例 | 正解 | 選択肢の出典 |
|---|---|---|---|
| アイコン→年 | 「この<アイコン>は、いつの出来事？」 | 対象出来事の`year` | 保有出来事の`year` |
| 人物→活躍年 | 「<偉人名>は何年に活躍した人でしょう？」 | 対象出来事の`year` | 保有出来事の`year` |
| 年→人物（2026-09-25文言修正） | 「yyyy年に活躍した偉人は？」 | 対象出来事の`title` | 保有出来事の`title` |
| 年→アイコン（2026-09-25追加） | 「yyyy年の偉人のゆかりのものはどれ？」 | 対象出来事の`icon`＋アイテム名 | 保有出来事のアイコン＋アイテム名 |
| 体の部位→アイコン（2026-09-25追加） | 「<体の部位>あたりにあるものは？」 | 対象出来事の`icon`＋アイテム名 | 保有出来事のアイコン＋アイテム名（体の部位の年に基づき20年以上離す） |
| アイコン→体の部位（2026-09-25追加） | 「<アイコン＋アイテム名>はどこ？」 | 対象出来事の`bodyPart` | 保有出来事の`bodyPart`（20年以上離す） |
| 人物→体の部位（2026-09-25追加） | 「<偉人名>が活躍したのはどこ？」 | 対象出来事の`bodyPart` | 保有出来事の`bodyPart`（20年以上離す） |
| 体の部位→人物（2026-09-25追加） | 「<体の部位>あたりで活躍したのは誰？」 | 対象出来事の`title` | 保有出来事の`title` |
| 体の部位→年代（2026-09-25追加） | 「<体の部位>あたりは何年？」 | 対象出来事の年に基づく10年幅の範囲（後述） | 保有出来事の年代範囲（20年以上離す） |
| 年代→体の部位（2026-09-25追加） | 「yyyy〜yyyy年はどこ？」 | 対象出来事の`bodyPart` | 保有出来事の`bodyPart`（20年以上離す） |
| アイコンの並び替え（2026-09-25追加） | 「<アイコン1>、<アイコン2>、<アイコン3>の人物が活躍した年の順番は？」 | 3人の活躍年が古い順になる並び方 | 3人の並び方（3! = 6通り）のうち、正解1通り＋誤答2通りをランダムに選んだ3択 |

保有出来事から無作為に3件選び、その3人のアイコンを（時系列順ではなく）無作為な順で問題文に並べる。選択肢は「アイコン→アイコン→アイコン」という並びの文字列3通り（正解の時系列順1つ＋残り5通りからランダムに選んだ誤答2つ）。他のパターンと異なり、保有件数が3件ちょうどでも出題できる（3件からの並び替えなので選択肢数の調整は不要）。

「アイテム名」は`iconReason`（9-2-2節）の先頭部分をそのまま流用する（8-3節の愛称と同じ抽出ロジック）。「年代範囲」は西暦を4-5節の10年刻みマッピングの最も近いポイントに丸め、その前後5年（例：1522年→1520年のポイント→「1515〜1525年」）として表示する。

体の部位を扱うパターン（表の網掛け対象：体の部位が問題文または選択肢に登場するもの）は、隣接する体の部位同士が紛らわしいため、**選択肢の元になる出来事（または年代範囲）どうしの西暦が最低20年以上離れるよう**、候補を貪欲法で絞り込んでから選択肢を構成する（`bodyPartMinSeparationYears`設定値）。保有件数が少なく20年以上離れた候補が十分に確保できない場合は、選択肢数がその分減る。

#### 10-1-2. 5問目：出来事の体の部位（2026-09-25全面改訂、旧・前後関係問題の◯×形式を廃止）

旧仕様では「これは<偉人名>より前/後の出来事である」という◯×形式と、「これより前/後の出来事はどれ？」という選択式の2形式を持っていたが、◯×形式を廃止し、以下の1形式に一本化した。

```
西暦：yyyy年
事件：<静的リストの見出し>

この事件が起きたのはどこ？
```

- 「事件」側の題材は、円環リストとは無関係に、専用の静的マスタ（`data/quiz/order_events.json`、10-4節）からランダムに1件選ぶ。ユーザーの保有状況に左右されないため、クイズ中に初めて見る情報になる（この部分は旧仕様から変更なし）。
- 正解は、その西暦を4-5節の身体部位マッピング（`data/quiz/body_points.json`、50箇所）の最も近いポイントに丸めたときの体の部位ラベル。
- 選択肢は、**保有出来事からではなく身体部位マッピングの50箇所全体から**選ぶ（保有件数に左右されず、常に4択）。10-1-1節と同様に、選択肢どうしの西暦が最低20年以上離れるよう絞り込む。
- 旧仕様で使っていた「功績（短い名詞句）」の静的マスタ（`data/quiz/person_actions.json`）は、◯×形式の廃止に伴い不要になったため削除した。

#### 10-1-3. 保有件数が少ない場合の選択肢数調整

レベル1直後など保有件数が少ない序盤では、選択肢用の誤答候補が十分に確保できないことがある。この場合、選択肢の総数を保有件数に応じて動的に減らす（例：保有3件なら3択、保有4〜5件なら4択、以降は上限4択程度を基本とする）。正解1件＋残りの保有出来事から重複なく誤答を抽出できる範囲で選択肢数を決定する。体の部位を扱うパターンでは、これに加えて10-1-1節の「20年以上離す」制約も同時に満たす必要があるため、選択肢数がさらに絞られることがある。

### 10-2. 合否判定・結果発表

- 5問中3問以上正解で合格。5問すべてに回答するまでは正誤を一切表示しない。
- 結果発表演出（詳細は10-5節）は、正解数のカウントアップ→結果カードの表示、という1〜2秒程度の短い流れとする。
- 表示するのは「レベルクリア表示＋合計点＋合否＋称号＋（合格時のみ）保有上限の解放数」で、各問の正誤内訳（見直し画面）は表示しない。
- **レベルクリア表示**（2026-09-25追加）：合否をひと目でわかりやすくするため、結果カードの最上部に大きく「Lv.X クリア！」（合格）または「Lv.X クリアならず」（不合格）と表示する。Xは挑戦していたレベル（合格した場合でもレベルアップ前の番号）。合格時はember寄りの色、不合格時はmutedな色にして視覚的にも区別する。
- 合格時：レベルが+1され、「あなたは<新しい称号>になった」という即時フィードバックを表示する。称号部分だけは太字にして目立たせる（2026-09-25追加）。さらにダイアログ下部に、保有上限が何件増えたかを知らせる文言（例：「記憶できる歴史が、あと1つ解放されました！」）を表示する（2026-09-25追加）。増加数はレベルごとに異なる（10-3節の「今回の追加数」）ため、常に+1とは限らず実際の増加数をそのまま表示する。
- 不合格時：レベルは変わらず、「あなたは<現在の称号>のままだ」というフィードバックを表示する（称号部分は同様に太字）。保有上限の解放文言は表示しない。レベルクリア表示は「ざんねん！」というやさしい言葉にする（2026-09-25追加、「Lv.X クリアならず」という表現から変更。「ならず」が小学生には読みにくいため）。さらにダイアログ下部に、合格条件を小学生にもわかる言葉で伝える一文（例：「5問中3問以上正解するとクリアだよ。つぎはがんばろう！」）を表示する（2026-09-25追加）。合格時の保有上限解放文言と同じ表示位置・同じ要素を使い、合否に応じて文言を出し分けている。数値は固定値ではなく実際の出題数・合格ラインをそのまま使う。

### 10-3. レベル・保有上限・称号テーブル

初期状態（円環リストが空）から歴史ピックアップフェーズを最初に行うとレベル1に入る。以降、クイズ合格ごとにレベルが上がり、円環に保有できる出来事の上限が段階的に開放される。26件（レベル15）で全偉人がそろい、コンプリートとなる。

| レベル | 今回の追加数 | 保有上限（累計） | 称号 |
|---|---|---|---|
| Lv.1 | （初回3件） | 3 | 記憶の見習い |
| Lv.2 | +1 | 4 | 円環の観察者 |
| Lv.3 | +1 | 5 | 円環の探求者 |
| Lv.4 | +2 | 7 | 時の道筋を辿る者 |
| Lv.5 | +3 | 10 | 記憶の織り手 |
| Lv.6 | +1 | 11 | 歴史の旅人 |
| Lv.7 | +2 | 13 | 円環の案内人 |
| Lv.8 | +1 | 14 | 記憶の番人 |
| Lv.9 | +3 | 17 | 時の目撃者 |
| Lv.10 | +1 | 18 | 歴史の編纂者 |
| Lv.11 | +2 | 20 | 円環の賢者 |
| Lv.12 | +1 | 21 | 記憶の継承者 |
| Lv.13 | +2 | 23 | 時代を渡る者 |
| Lv.14 | +1 | 24 | 歴史の守護者 |
| Lv.15 | +2 | 26（コンプリート） | 宮殿の主 |

称号はレベル番号から一意に導出できるため、称号そのものは端末側に別途保存しない（9-5節）。

### 10-4. 5問目用 静的出来事マスタ（2026-09-22実装、2026-09-25用途変更、同日文体も改訂）

汎用テーマ・偉人テーマのどちらの保有状況にも依存しない、独立した静的データとして持つ。専門用語を避け、生活・衛生・流行・常識の転換など「歴史の知識がなくても直感的にわかる」出来事を中心に選定した。26人の偉人の実際の活躍年（1522〜1994年の間の26個の年）とは重複しないように選んでいる。実データは`data/quiz/order_events.json`（34件、各要素は`id`・`year`・`headline`の3フィールド）に格納済み。当初は前後関係問題（旧仕様）用だったが、2026-09-25の改訂で5問目は「この事件が起きたのはどこ（体の部位）」を問う形式（10-1-2節）に変わったため、現在はそちらで使っている。

文言は当初「ニュース見出し風」（体言止め・煽り文句・カギカッコの強調など）で書いていたが、5問目の問題文としては何が起きたのか直感的に伝わりにくいというフィードバックを受け、2026-09-25に**平易な一文で出来事を説明する文体**へ全面的に書き直した（見出し風の凝った表現をやめ、「〜が発明される」「〜が発見される」のような素直な言い切りに統一）。

| 西暦 | 事件 |
|---|---|
| 1517 | 教会のやり方に疑問を投げかける文書が発表され、宗教改革が始まる |
| 1533 | 上流階級の食卓に、フォークを使って食事をする習慣が広まり始める |
| 1543 | 地球が太陽の周りを回っているという新しい説が発表される |
| 1569 | 世界の陸と海を正確に描き写せる地図の描き方が発表される |
| 1589 | 靴下を機械で編む道具が発明され、大量生産できるようになる |
| 1596 | 水を流して汚れを流すトイレの仕組みが考え出される |
| 1618 | お茶がヨーロッパに伝わり、貴族の間で飲まれ始める |
| 1652 | コーヒーを飲みながらおしゃべりできる店がロンドンに登場する |
| 1656 | 振り子を使うことで、時計が正確に時を刻めるようになる |
| 1676 | 水の中にいる、目に見えないほど小さな生き物が発見される |
| 1733 | 布を織る作業を速くする道具が発明される |
| 1762 | 賭け事をしながら片手で食べられるよう、パンに具を挟んだ食べ物が考え出される |
| 1791 | 世界共通の長さの単位を作ろうという計画が始まる |
| 1810 | 瓶に詰めて密封することで、食べ物を長く保存できるようになる |
| 1816 | 胸に当てて体の中の音を聞く道具が発明される |
| 1835 | 長年信じられてきた治療法に、実は効果がないことが分かる |
| 1839 | 目の前の景色をそのまま写し取る技術が発表される |
| 1846 | 手術中に痛みを感じさせない方法が初めて成功する |
| 1858 | 保存食を開けるための専用の道具が発明される |
| 1873 | 丈夫な生地に金属の鋲を打ったズボンが作られ、労働者の間で広まる |
| 1886 | 甘い炭酸の飲み物が、体に良い薬として売り出される |
| 1892 | 板状のチョコレートが、手軽なお菓子として売り出される |
| 1895 | 体を切らずに中の骨を写し出せる光線が発見される |
| 1903 | エンジンの力で空を飛ぶ機械が初めて成功する |
| 1913 | 流れ作業で自動車を作る方法が始まり、値段が安くなる |
| 1920 | 音楽やニュースが自宅に届く、ラジオ放送が始まる |
| 1935 | 破れにくい新しい繊維が発明され、女性の靴下に使われ始める |
| 1946 | 温めるだけで料理ができる調理器具が発明される |
| 1958 | 買い物の支払いをカード一枚で済ませられる仕組みが広まり始める |
| 1971 | 計算をする機械が、家庭でも買えるくらい小さく安くなる |
| 1975 | 見たい番組を録画して後から見られる機器が登場する |
| 1983 | 持ち運べる電話が初めて商品化される |
| 1990 | 世界中のコンピューターをつなぐ新しい仕組みが考え出される |
| 1998 | 知りたい情報をすぐ探せる検索サービスが登場する |

*年号は広く知られているものを採用しているが、実装前に出典を用いて再検証することを推奨する（要検証、2026-09-22時点）。*

### 10-5. 演出（音楽・BGMは実装しない）

- **出題時**：問題カードが円環UIから飛び出すように、軽く回転しながら中央にフェードインする。選択肢をタップした瞬間は、その選択肢がふわっと光って軽く隆起する程度のフィードバックを返す。
- **結果発表**：5問終了後、正解数が0→Xへ素早くカウントアップ（1〜2秒程度）した後、称号カードがフェードインする。合格時は称号カードの周囲に控えめな光の粒子を一瞬だけ舞わせ、不合格時はカウントアップのみに留めて演出の対比をつくる。
- **円環UIへの反映**：クイズ合格でレベルが上がり、その後の歴史ピックアップ画面で新しい出来事を選んで円環画面に戻ると、追加された出来事の位置が1〜2秒ほど明滅して光る（実装上は「ピックアップ完了→円環画面に戻る」タイミングで発火する。クイズ合格そのものはレベルの保有上限を開放するだけで、その場で出来事が増えるわけではないため）。

### 10-6. 実装状況・残る未確定事項（2026-09-23更新）

- 本章の内容（レベル制・出題ロジック・結果演出）は`index.html`側にJS実装済み。データは`data/quiz/patterns.json`（出題パターン定義）・`data/quiz/order_events.json`（5問目用静的マスタ、34件）・`data/quiz/body_points.json`（身体部位マッピング、50件、4-5節）を実行時に`fetch`で読み込む（2026-09-25改訂：`data/quiz/person_actions.json`は◯×形式の廃止に伴い削除した）。
- レベル管理は`rekishiNoKyuden.level.v1`というキーでローカルストレージに保存する（9-5節）。既存の円環リストの件数がレベルの保有上限を上回っている場合（過去バージョンのデータ等）は、起動時にレベルを保有件数に見合うところまで自動的に引き上げる（レベルを下げることはない）。
- レベル15（26件コンプリート）到達後のホーム画面表示（「クイズ」ボタンのままにするか、専用のコンプリート表示にするか）は保留。現状の実装では「クイズに挑戦」ボタンのまま据え置き、クイズは無制限に周回できる。

---

*本文書は設計書の概要編として作成。次フェーズでは画面設計・API設計・データモデルの定義へと移行する。*
